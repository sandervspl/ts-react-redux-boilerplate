export interface IColors {
    black: string;
    white: string;
    primary: string;
    text: string;
}

export interface IFonts {
    futura: string;
}

export interface ITheme {
    color: IColors;
    font: IFonts;
}
